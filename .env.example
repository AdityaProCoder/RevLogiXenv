#
# RevLogiXenv example environment file
#
# Copy to `.env` and fill what you need.
#

# Hackathon / inference.py (OpenAI-compatible endpoints)
API_BASE_URL=
MODEL_NAME=
HF_TOKEN=

# Optional: LLM baseline runner (run_baseline.py --mode llm)
OPENAI_API_KEY=
VERTEX_PROJECT_ID=
VERTEX_LOCATION=us-central1
